<%= form_for(killmail) do |f| %>
  <% if killmail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(killmail.errors.count, "error") %> prohibited this killmail from being saved:</h2>

      <ul>
      <% killmail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
